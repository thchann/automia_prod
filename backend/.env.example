# Comma-separated list of browser origins allowed to call this API (no trailing slashes).
# Include every dev URL you use (localhost, LAN IP, production domains).
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://10.240.10.18:8080
