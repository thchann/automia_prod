/**
 * Future: load makes, models, and car types from API or config.
 * Deferred per product roadmap — use free-text fields on `Car` until then.
 */
export const CAR_MAKES_PLACEHOLDER: readonly string[] = [];
export const CAR_MODELS_BY_MAKE_PLACEHOLDER: Readonly<Record<string, readonly string[]>> = {};
export const CAR_TYPES_PLACEHOLDER: readonly string[] = [];
